# Access Manager Bot - Production
# Connects to external PostgreSQL/Redis via environment variables

services:
  access-manager-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: access-manager-bot
    env_file:
      - .env
    ports:
      - "${HEALTH_PORT:-8080}:8080"
    restart: unless-stopped
